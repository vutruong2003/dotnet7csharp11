namespace CSharpEleven.MinorChanges;
internal class NumbericIntPtr
{
    public static void Execute()
    {
        IntPtr intPtr = IntPtr.Zero;
        nint otherIntPtr = nint.Zero;

        UIntPtr uintPtr = UIntPtr.Zero;
        nuint otherUIntPtr = nuint.Zero;
    }
}
